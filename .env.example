# Root env for docker-compose interpolation.
# Copy to .env (gitignored) and fill in real values — never commit secrets.

# Commerce database connection (shared with the portal).
# docker-compose.yml interpolates this into each site's environment.
COMMERCE_DATABASE_URL=postgresql://user:pass@host:5432/commerce
