node_modules/
.next/
dist/
.env
.env.local
.env.*.local
*.tsbuildinfo
orchestrator/.ip-allocations.json
.claude/

# Scraper state (Phase 0–2)
.scraper-profile/
scripts/.work/

# Local audit workspace (reports, crawls, large JSONL data) — not versioned
/scripts/
.worktrees/

# parity harness output (screenshots + summary)
tools/parity/report/
# tab-panel captures (baseline/after/diff PNGs) — regenerated on demand
tools/parity/tabs/
